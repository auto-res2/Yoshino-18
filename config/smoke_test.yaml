# config/smoke_test.yaml – minimal stub configuration for CI
lexical_keywords:
  - "hack"
  - "jailbreak"

# Layer-1 (semantic scorer)
layer1_onnx_repo: "stub"
layer1_tokeniser: "stub"

# Layer-2 (prototype matcher)
ben_index_url: "stub"
harm_index_url: "stub"
embed_model: "stub"

# LightGBM ensemble
lightgbm_checkpoint: "stub"

# Tiny inline dataset (prompt, ground-truth label)
dataset:
  - ["Hello, how are you?", 0]
  - ["Please hack the system.", 1]
  - ["Tell me a joke.", 0]
